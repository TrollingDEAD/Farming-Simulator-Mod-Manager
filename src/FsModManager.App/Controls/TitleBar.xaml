<UserControl x:Class="FsModManager.App.Controls.TitleBar"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
             Height="40"
             Background="{StaticResource SidebarBrush}">
    <Grid>
        <!-- App identity; sits in the WindowChrome caption region so it drags/double-click-maximizes -->
        <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="12,0,0,0">
            <TextBlock Text="🚜" FontSize="15" VerticalAlignment="Center" />
            <TextBlock Text="FS Mod Manager"
                       Margin="10,0,0,0"
                       VerticalAlignment="Center"
                       FontSize="13"
                       FontWeight="SemiBold" />
        </StackPanel>

        <!-- Interactive elements must opt back in to hit-testing inside the caption region -->
        <StackPanel Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    shell:WindowChrome.IsHitTestVisibleInChrome="True">
            <Button x:Name="MinimizeButton"
                    Style="{StaticResource TitleBarButtonStyle}"
                    Content="&#xE921;"
                    ToolTip="Minimize"
                    Click="OnMinimizeClick" />
            <Button x:Name="MaximizeButton"
                    Style="{StaticResource TitleBarButtonStyle}"
                    Content="&#xE922;"
                    ToolTip="Maximize"
                    Click="OnMaximizeClick" />
            <Button x:Name="CloseButton"
                    Style="{StaticResource TitleBarCloseButtonStyle}"
                    Content="&#xE8BB;"
                    ToolTip="Close"
                    Click="OnCloseClick" />
        </StackPanel>
    </Grid>
</UserControl>
